steps:
  - name: 'gcr.io/cloud-builders/docker'
    dir: ''
    args: [ 'build', '.', '-t', '${_REGION}-docker.pkg.dev/${_PROJECT}/${_REPO}/${_IMAGE}', '-f', './Dockerfile' ]
    id: 'build-dbt'
images:
  - '${_REGION}-docker.pkg.dev/${_PROJECT}/${_REPO}/${_IMAGE}'